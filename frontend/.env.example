# Environment Variables Template for Local Development
# Copy this file to .env.local and fill in your actual values
# .env.local is git-ignored and will not be committed

# ============================================
# Convex Configuration (Development)
# ============================================
# These are auto-generated when you run `npx convex dev`
# Replace with your actual Convex deployment values

# Deployment name (format: dev:deployment-name)
CONVEX_DEPLOYMENT=dev:your-deployment-name

# Convex deployment URL (for Convex client)
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Convex site URL (for Better Auth - must match your Convex deployment)
VITE_CONVEX_SITE_URL=https://your-deployment.convex.site

# ============================================
# Cloudinary Configuration
# ============================================
# Get these from your Cloudinary Dashboard: https://console.cloudinary.com/

VITE_CLOUDINARY_CLOUD_NAME=your_cloud_name
VITE_CLOUDINARY_API_KEY=your_api_key
VITE_CLOUDINARY_UPLOAD_PRESET=your_preset_name

# Optional: Cloudinary URL (for direct uploads)
# VITE_CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name

# ============================================
# Demo / Admin (optional - for client-side role checks)
# ============================================
# Demo user email - if set, user with this email sees demo banner and can access admin UI (changes not persisted)
# VITE_DEMO_USER_EMAIL=demo@example.com
# Admin user email - if set, user with this email is treated as admin in the UI
# VITE_ADMIN_EMAIL=travis@nerdycoder.com
# Demo user password - if set, user with this password can access demo mode (changes not persisted)
# VITE_DEMO_USER_PASSWORD=demo124
# Admin user password - if set, user with this password is treated as admin in the UI
# VITE_ADMIN_PASSWORD=Noel1124#$#$#$


# ============================================
# AI Services (Optional)
# ============================================
# Google Gemini API Key (for AI-powered media analysis)
# Get from: https://makersuite.google.com/app/apikey
VITE_GEMINI_API_KEY=your_gemini_api_key

# ============================================
# Notes
# ============================================
# - All VITE_ prefixed variables are exposed to the browser
# - Never commit .env.local to git (it's in .gitignore)
# - For production, set these in Vercel Dashboard → Settings → Environment Variables
# - For Convex backend variables (like SITE_URL), set them in Convex Dashboard
